use concrete_core::prelude::*;

fn main() {

    // encrypt k-bit integer into k-bit ciphertext
    // allow for addition of l independent samples
    // evaluate custom LUT of 2^{k-1} entries (the remaining values consist of the LUT's negacyclic extension)

    println!("Bye, world!");
}
